# v0.2.0 Manual Validation

## 1. 目的

记录 `v0.2.0` 发布准备阶段的真实手工验证证据，避免项目只停留在“测试理论上应该通过”。

## 2. 当前验证记录

### 2026-03-13 v0.2.0 Release Gate

- 执行人：Codex / Eng Lead
- 分支：`main`
- 版本：`v0.2.0`
- 运行方式：workspace 测试 + 真实 listener smoke + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0.sh`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `git diff --check`
- `README.md`、`CHANGELOG.md`、`docs/roadmap.md`、`docs/release/v0.2.0-release-notes.md` 已同步到正式发布状态
- Markdown 文档链接已切换到 GitHub 可用的相对路径
- release workflow 仍将由 `.github/workflows/release.yml` 在 `v*` tag push 后自动构建 `fa-server-linux-amd64.tar.gz` 并创建 GitHub release

结果：

- 状态：`Ready for Release`
- 真实 listener smoke：通过，监听地址 `127.0.0.1:8000`
- smoke data dir：`/Users/admin/Documents/WORK/ai/fa/sandbox/fa-v0.2.0-smoke-2058115355`，执行成功后已自动清理
- sandbox-safe smoke：通过，使用项目内 `sandbox/`
- 备注：当前 release gate 已完整通过，本地剩余动作仅为提交 release commit、创建 `v0.2.0` tag 并推送到 GitHub

### 2026-03-13 Alert Cluster Monitoring Linked Follow-up Aggregates

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 alert cluster monitoring linked follow-up aggregate 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `GET /api/v1/alert-cluster-monitoring` 现在会返回 `linked_follow_up_clusters / unlinked_follow_up_clusters / accepted_follow_up_clusters / unaccepted_follow_up_clusters / follow_up_escalation_clusters`
- `GET /api/v1/alert-cluster-monitoring` 现在会返回 `follow_up_coverage_counts / follow_up_sla_status_counts` 两组 linked follow-up 聚合 bucket
- monitoring 现在可直接回答“多少 cluster 已有人接、多少仍未接单、多少 linked follow-up 已升级”
- `source_system=scada` 等现有过滤仍会同步收敛新的 linked follow-up monitoring 聚合结果
- sandbox-safe file mode 重启后仍可回读这些 linked follow-up monitoring aggregate 字段
- 现有 alert triage task detail、alert cluster queue / linkage / triage filters、follow-up queue / monitoring、handoff receipt queue / monitoring 与高风险异常主链仍保持通过，不受 monitoring aggregate 扩展影响

结果：

- 状态：通过
- 备注：本阶段仍复用 repository 扫描，不引入 accepted-owner load bucket、dedicated projection 或 cluster dashboard 专属 endpoint

### 2026-03-13 Alert Cluster Linked Follow-up Triage Filters

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 alert cluster linked follow-up triage filter 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `GET /api/v1/alert-clusters` 现在支持 `follow_up_owner_id / unaccepted_follow_up_only / follow_up_escalation_required` 三个 linked follow-up triage 过滤
- `follow_up_owner_id=worker_1001` 可只返回已被 accepted owner 接手的 cluster
- `unaccepted_follow_up_only=true` 可只返回仍存在未 accepted follow-up 的 cluster
- `follow_up_escalation_required=true` 可只返回 linked follow-up 已进入升级态的 cluster
- `GET /api/v1/alert-cluster-monitoring` 会复用同一组 linked follow-up triage 过滤语义
- sandbox-safe file mode 重启后仍可回读 queue 与 monitoring 的 linked follow-up triage 过滤结果
- 现有 alert triage task detail、alert cluster linkage、follow-up queue / monitoring、handoff receipt queue / monitoring 与高风险异常主链仍保持通过，不受 queue/filter 扩展影响

结果：

- 状态：通过
- 备注：本阶段只补 cross-task triage filter，不引入新的 cluster monitoring bucket、projection 或 cluster follow-up 专属 endpoint

### 2026-03-13 Alert Cluster Follow-up Linkage

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 alert cluster follow-up linkage 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `GET /api/v1/alert-clusters` 现在会在每条 cluster item 上返回最小 `linked_follow_up` 摘要
- `linked_follow_up` 当前会返回 `total_items / open_items / accepted_items / unaccepted_items / follow_up_ids / accepted_owner_ids / worst_effective_sla_status`
- linkage 优先支持显式 `follow_up_item.source_kind=alert_cluster` 且 `source_refs` 指向 `cluster_id`
- 现有单 cluster `alert triage` task 仍可通过兼容回退把旧版 follow-up 挂到对应 cluster 上
- `follow-up-items/{follow_up_id}/accept-owner` 后，cluster queue 会同步反映 accepted owner 与 `worst_effective_sla_status`
- sandbox-safe file mode 重启后仍可回读 linked follow-up 摘要
- 现有 alert triage task detail、alert cluster monitoring、follow-up queue / monitoring、handoff receipt queue / monitoring 与高风险异常主链仍保持通过，不受 linkage 读层扩展影响

结果：

- 状态：通过
- 备注：本阶段只补 queue item linkage 读摘要，不引入 cluster 专属 follow-up endpoint、dedicated projection 或新的写侧状态机

### 2026-03-13 Alert Cluster Monitoring

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 alert cluster monitoring 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `GET /api/v1/alert-cluster-monitoring` 可返回最小 alert cluster monitoring 聚合视图
- monitor 会返回 `total / open / escalation_candidate / high_severity / active_window / stale_window / next_window_end_at` 核心汇总字段
- monitor 会返回 `cluster_status / source_system / severity_band / triage_label / owner_role / window_state / task_risk / task_priority` 八组最小 bucket 统计
- `GET /api/v1/alert-cluster-monitoring?source_system=scada` 可只返回 `scada` 来源 cluster backlog 的聚合监控结果
- `GET /api/v1/alert-cluster-monitoring?open_only=true` 可只返回仍处于 open backlog 的 cluster 聚合监控结果
- sandbox-safe file mode 重启后仍可回读 monitoring 汇总与过滤结果
- 现有 alert triage task detail、alert cluster queue、follow-up queue / monitoring、handoff receipt queue / monitoring 与高风险异常主链仍保持通过，不受 monitoring read API 影响

结果：

- 状态：通过
- 备注：本阶段仍复用 repository 扫描，不引入 dedicated projection、cluster aging trend 或真实 ingestion API

### 2026-03-13 Alert Cluster Queue

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 alert cluster queue 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `GET /api/v1/alert-clusters` 可跨任务聚合返回 `alert triage` 的 alert cluster queue
- queue 默认会把 `escalation_candidate=true` 且高 severity 的 `andon` cluster 排在 `scada` 阈值类 cluster 前
- `GET /api/v1/alert-clusters?source_system=scada` 可只返回 `scada` 来源 cluster
- `GET /api/v1/alert-clusters?line_id=line_mix_02` 与 `?triage_label=sustained_threshold_review` 可按产线和 triage 形态过滤
- `GET /api/v1/alert-clusters?escalation_candidate=true` 可只返回升级候选 cluster
- `GET /api/v1/alert-clusters?window_from=...&window_to=...` 会按 cluster window overlap 返回命中的 cluster
- `GET /api/v1/alert-clusters?open_only=true` 可只返回仍处于 `open` 的 cluster
- sandbox-safe file mode 重启后仍可回读 alert cluster queue 与过滤结果
- 现有 alert triage task detail、follow-up queue / monitoring、handoff receipt queue / monitoring 与高风险异常主链仍保持通过，不受新 queue read API 影响

结果：

- 状态：通过
- 备注：本阶段仍复用 repository 扫描，不引入 dedicated alert-cluster projection、monitoring 视图或真实 ingestion API

### 2026-03-13 Alert Cluster Inference

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 alert cluster inference 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- 现有 `andon` alert triage draft 现在会稳定输出 `line_pack_04`
- `scada` 阈值类 alert triage 请求现在可生成 `sustained_threshold_review` cluster draft
- 该 draft 会推断 `source_system=scada / line_id=line_mix_02 / recommended_owner_role=maintenance_engineer`
- cluster window 会根据 triage 形态切换到更长的 `15m` 窗口
- sandbox-safe file mode 重启后仍可回读 richer alert cluster inference 结果
- 现有高风险异常主链、follow-up queue、follow-up monitoring、handoff receipt queue / monitoring 与原有 alert triage 主链仍保持通过，不受 inference 扩展影响

结果：

- 状态：通过
- 备注：本阶段仍只增强 task-scoped draft inference，不引入 `alert cluster` queue、ingestion API 或 `Scada` mock connector

### 2026-03-13 Handoff Receipt Monitoring

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 handoff receipt monitoring 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `GET /api/v1/handoff-receipt-monitoring` 可返回最小 handoff receipt monitoring 聚合视图
- monitor 会返回 `total / open / acknowledged / unacknowledged / overdue / exception / escalated / next_ack_due_at` 核心汇总字段
- monitor 会返回 `effective_status / receiving_role / ack_window / task_risk / task_priority` 五组最小 bucket 统计
- `GET /api/v1/handoff-receipt-monitoring?escalated_only=true` 可只返回已升级 receipt backlog 的聚合监控结果
- sandbox-safe file mode 重启后仍可回读 monitoring 汇总与过滤结果
- 现有高风险异常主链、follow-up queue、follow-up monitoring、handoff receipt queue 与 alert triage cluster 主链仍保持通过，不受 monitoring read API 影响

结果：

- 状态：通过
- 备注：本阶段仍复用 repository 扫描，不引入 dedicated projection、aging trend slice 或 `expired` 持久化状态机

### 2026-03-13 Follow-up SLA Monitoring

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 follow-up SLA monitoring 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `GET /api/v1/follow-up-monitoring` 可返回最小 follow-up SLA monitoring 聚合视图
- monitor 会返回 `total / open / accepted / unaccepted / blocked / overdue / escalation_required / next_due_at` 核心汇总字段
- monitor 会返回 `source_kind / owner_role / effective_sla_status / task_risk / task_priority` 五组最小 bucket 统计
- `GET /api/v1/follow-up-monitoring?source_kind=alert_triage` 可只返回 alert triage backlog 的聚合监控结果
- sandbox-safe file mode 重启后仍可回读 monitoring 汇总与过滤结果
- 现有高风险异常主链、follow-up owner queue、queue triage filters、handoff receipt queue 与 alert triage cluster 主链仍保持通过，不受 monitoring read API 影响

结果：

- 状态：通过
- 备注：本阶段仍复用 repository 扫描，不引入 dedicated projection、aging worker 或 backlog history snapshot

### 2026-03-13 Handoff Receipt Queue

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 handoff receipt queue 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `GET /api/v1/handoff-receipts` 可跨任务聚合返回 `shift handoff` 的 cross-shift receipt queue
- queue 默认会把超时未确认的 `published` receipt 排在带 exceptions 的已确认 receipt 前
- `GET /api/v1/handoff-receipts?overdue_only=true` 可只返回超时未确认的 receipt
- `GET /api/v1/handoff-receipts?has_exceptions=true` 与 `?escalated_only=true` 可只返回带异议或已升级的 receipt
- `GET /api/v1/handoff-receipts?shift_id=...` 与 `?receiving_actor_id=worker_1101` 可按交接窗口和接收人过滤
- sandbox-safe file mode 重启后仍可回读 cross-shift receipt queue 与过滤结果
- 现有高风险异常主链、follow-up owner queue、handoff receipt acknowledgement / escalation 主链与 alert triage cluster 主链仍保持通过，不受 receipt queue read API 影响

结果：

- 状态：通过
- 备注：本阶段先复用 repository 扫描做最小 cross-shift receipt queue，尚未引入 dedicated projection、receipt aging monitor 或 `expired` 持久化状态机

### 2026-03-13 Follow-up Queue Triage Filters

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 follow-up queue triage filters 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `GET /api/v1/follow-up-items` 现在支持 `blocked_only / escalation_required / due_before / risk / priority` 过滤
- `risk=high&priority=expedited&due_before=<alert_due_at>` 可只返回 `alert triage` follow-up item
- `blocked_only=true` 可只返回被阻塞的 `shift handoff` follow-up item
- `escalation_required=true` 可只返回需要升级的 `alert triage` follow-up item
- sandbox-safe file mode 重启后仍可回读 triage filter 结果
- 现有高风险异常主链、follow-up owner acceptance、handoff receipt acknowledgement / escalation 主链与 alert triage cluster 主链仍保持通过，不受 queue filter 扩展影响

结果：

- 状态：通过
- 备注：本阶段仍然复用 repository 扫描，不引入 dedicated projection、item completion action 或独立 SLA monitor

### 2026-03-13 Follow-up Owner Queue

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 follow-up owner queue 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `GET /api/v1/follow-up-items` 可跨任务聚合返回 `shift handoff` 与 `alert triage` 的 follow-up owner queue
- queue 默认会把更早到期的 `alert triage` item 排在 `shift handoff` item 前
- `GET /api/v1/follow-up-items?owner_id=worker_1101` 可只返回已被 incoming shift supervisor 接手的 item
- `GET /api/v1/follow-up-items?source_kind=alert_triage` 可只返回告警分诊 follow-up item
- sandbox-safe file mode 重启后仍可回读 cross-task follow-up queue 与过滤结果
- 现有高风险异常主链、follow-up owner acceptance、handoff receipt acknowledgement / escalation 主链与 alert triage cluster 主链仍保持通过，不受 queue read API 影响

结果：

- 状态：通过
- 备注：本阶段先复用 repository 扫描做最小 queue read，尚未引入 dedicated projection、blocked/risk filter 或 item completion state machine

### 2026-03-12 Follow-up Owner Acceptance

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 follow-up owner acceptance 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `shift handoff` 与 `alert triage` 的 seeded `follow_up_item` 可通过 `follow-up-items/{follow_up_id}/accept-owner` action 进入 `accepted`
- `recommended_owner_role` 不匹配的 actor 会被拒绝
- 已接受的 item 会拒绝重复 acceptance
- `shift handoff` receipt summary 会在 owner acceptance 后把 `unaccepted_follow_up_count` 收敛到 `0`
- sandbox-safe file mode 重启后仍可回读 accepted follow-up 与对应 `follow_up_owner_accepted` audit event
- 现有高风险异常主链、handoff receipt acknowledgement / escalation 主链与 alert triage cluster 主链仍保持通过，不受 follow-up acceptance action 影响

结果：

- 状态：通过
- 备注：本阶段只补最小显式 owner acceptance action，尚未引入 item assignment、cross-task owner queue 或 overdue projection

### 2026-03-12 Handoff Receipt Escalation for Shift Handoff

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 handoff receipt escalation 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `shift handoff` receipt 可通过 `handoff-receipt/escalate` action 从 `acknowledged_with_exceptions` 进入 `escalated`
- `handoff_receipt_summary` 会返回非空 `status`，且保留 `exception_flag`
- 非 `acknowledged_with_exceptions` 状态的 receipt 会拒绝 escalation
- 不匹配发送侧 accountable 角色的 actor 会被拒绝
- sandbox-safe file mode 重启后仍可回读 escalated receipt 与对应 `handoff_receipt_escalated` audit event
- 现有高风险异常主链、shift handoff acknowledgement 主链与 alert triage 主链仍保持通过，不受 escalation action 影响

结果：

- 状态：通过
- 备注：本阶段只补最小显式 escalation action，尚未引入 overdue queue 或跨班次 escalation projection

### 2026-03-12 Handoff Receipt Acknowledgement for Shift Handoff

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 handoff receipt acknowledgement 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `shift handoff` receipt 可通过 `handoff-receipt/acknowledge` action 从 `published` 进入 `acknowledged`
- 提供 `exception_note` 时，receipt 可进入 `acknowledged_with_exceptions`
- `handoff_receipt_summary` 会返回非空 `status / acknowledged_at / exception_flag`
- 不匹配 `receiving_role` 的 actor 会被拒绝
- sandbox-safe file mode 重启后仍可回读 acknowledged receipt 与对应 audit event
- 现有高风险异常主链与 alert triage 主链仍保持通过，不受 acknowledgement action 影响

结果：

- 状态：通过
- 备注：本阶段只补最小显式 acknowledgement action，尚未引入 receipt overdue / escalation projection

### 2026-03-12 Alert Triage Follow-up Draft Seeding

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 alert triage follow-up draft seeding 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `alert triage` 请求在 `tasks/intake` 返回 1 条 seeded `follow_up_item`
- `GET /api/v1/tasks/{task_id}` 可回读该 seeded `follow_up_item`
- `follow_up_summary` 会返回非零 `total_items / open_items`
- 同一 `alert triage` 请求仍会保留 1 条 seeded `alert_cluster_draft`
- sandbox-safe file mode 重启后仍可回读该 seeded alert triage follow-up draft
- 现有 `shift handoff` 与默认高风险异常主链仍保持通过，不受 alert triage follow-up seeding 影响

结果：

- 状态：通过
- 备注：本阶段只为 `alert triage` 做受控 follow-up draft seeding，尚未引入 owner acceptance / assignment action

### 2026-03-12 Alert Cluster Draft Seeding for Alert Triage

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 alert cluster draft seeding 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `alert triage` 请求在 `tasks/intake` 返回 1 条 seeded `alert_cluster_draft`
- `GET /api/v1/tasks/{task_id}` 可回读该 seeded `alert_cluster_draft`
- `alert_triage_summary` 会返回非零 `total_clusters / open_clusters / escalation_candidate_count`
- sandbox-safe file mode 重启后仍可回读该 seeded alert cluster draft
- 现有 `shift handoff` 与高风险异常主链仍保持通过，不受 alert draft seeding 影响

结果：

- 状态：通过
- 备注：本阶段只为 `alert triage` 做受控 cluster draft seeding，尚未引入事件写入接口或独立 cluster query API

### 2026-03-12 Handoff Receipt Draft Seeding for Shift Handoff

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 handoff receipt draft seeding 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `shift handoff` 请求在 `tasks/intake` 返回 non-null `handoff_receipt`
- `GET /api/v1/tasks/{task_id}` 可回读该 seeded `handoff_receipt`
- `handoff_receipt_summary` 会返回非零 `covered_follow_up_count / unaccepted_follow_up_count`
- 现有高风险异常主链仍保持通过，不受 receipt seeding 影响

结果：

- 状态：通过
- 备注：本阶段只为 `shift handoff` 做受控 receipt draft seeding，尚未引入 acknowledgement action

### 2026-03-12 Follow-up Draft Seeding for Shift Handoff

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 follow-up draft seeding 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `shift handoff` 请求在 `tasks/intake` 返回 1 条 seeded `follow_up_item`
- `GET /api/v1/tasks/{task_id}` 可回读该 seeded `follow_up_item`
- 现有高风险异常主链仍保持通过，不受 follow-up seeding 影响

结果：

- 状态：通过
- 备注：本阶段只为 `shift handoff` 做受控 draft seeding，尚未扩展到 `handoff_receipt` 或 `alert_cluster_drafts`

### 2026-03-12 Alert Triage Task Detail Schema Cut

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 alert cluster schema cut 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `tasks/intake` 返回 `alert_cluster_drafts / alert_triage_summary`
- `GET /api/v1/tasks/{task_id}` 返回 `alert_cluster_drafts / alert_triage_summary`
- file / SQLite repository 可回读新增的 alert cluster task detail 字段
- 旧任务 JSON 缺少新字段时仍能兼容反序列化

结果：

- 状态：通过
- 备注：本阶段只完成空 schema cut 和兼容性验证，尚未填充非空 cluster draft

### 2026-03-12 Shift Handoff Receipt Task Detail Schema Cut

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 handoff receipt schema cut 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `tasks/intake` 返回 `handoff_receipt / handoff_receipt_summary`
- `GET /api/v1/tasks/{task_id}` 返回 `handoff_receipt / handoff_receipt_summary`
- file / SQLite repository 可回读新增的 handoff receipt task detail 字段
- 旧任务 JSON 缺少新字段时仍能兼容反序列化

结果：

- 状态：通过
- 备注：本阶段只完成空 schema cut 和兼容性验证，尚未填充非空 receipt draft

### 2026-03-12 Follow-up Task Detail Schema Cut

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 follow-up schema cut 变更
- 运行方式：workspace 测试 + 项目内 `sandbox/`
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `cargo fmt --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke_v0_2_0_sandbox.sh`
- `tasks/intake` 返回 `follow_up_items / follow_up_summary`
- `GET /api/v1/tasks/{task_id}` 返回 `follow_up_items / follow_up_summary`
- file / SQLite repository 可回读新增的 follow-up task detail 字段
- 旧任务 JSON 缺少新字段时仍能兼容反序列化

结果：

- 状态：通过
- 备注：本阶段只完成空 schema cut 和兼容性验证，尚未填充非空 follow-up draft

### 2026-03-12 Evidence + Release Readiness Baseline

- 执行人：Codex / Eng Lead
- 基线 commit：`d9be6a3`
- 运行方式：`FA_DATA_DIR` 文件模式
- 默认地址：`127.0.0.1:8000`
- smoke data dir：`/tmp/fa-v0.2.0-smoke-2364032436`

已验证：

- `intake` 返回 `awaiting_approval`
- 任务详情返回 `evidence`
- `GET /api/v1/tasks/{task_id}/evidence` 返回结构化证据快照
- `planned_task.task.plan.governance` 返回 responsibility matrix 与 approval strategy
- `GET /api/v1/tasks/{task_id}/governance` 返回任务级治理视图
- 服务重启后任务与 evidence 可回读
- 审批接口会拒绝不匹配 `required_role` 的审批人角色
- `approve -> execute -> complete` 主链成立
- 任务级审计回放成立
- 按 `correlation_id` 过滤审计事件成立

结果：

- 状态：通过
- 备注：`bash scripts/smoke_v0_2_0.sh` 已完整通过，覆盖 `intake -> evidence -> governance -> restart -> approve -> execute -> complete -> audit query`

### 2026-03-12 Approval Role Enforcement Baseline

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1`
- 运行方式：`FA_DATA_DIR` 文件模式
- 默认地址：`127.0.0.1:8000`
- smoke data dir：`/tmp/fa-v0.2.0-smoke-1511030520`

已验证：

- `cargo fmt --all`
- `cargo test --workspace`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `bash scripts/smoke_v0_2_0.sh`
- 高风险任务审批要求 `Safety Officer`
- 不匹配 `required_role` 的审批请求会被拒绝
- 匹配角色的 `approve -> execute -> complete` 主链仍成立

结果：

- 状态：通过
- 备注：服务层测试和真实 smoke 都已覆盖审批角色强校验

### 2026-03-12 Sandbox-Safe Smoke Baseline

- 执行人：Codex / Eng Lead
- 基线 commit：`7b24ce1` 之后的 sandbox-safe smoke 变更
- 运行方式：项目内 `sandbox/` + 进程内 HTTP 路由验证
- sandbox 根目录：`/Users/admin/Documents/WORK/ai/fa/sandbox`

已验证：

- `bash scripts/smoke_v0_2_0_sandbox.sh`
- 文件模式持久化在项目内 `sandbox/` 下可安全创建
- 不依赖本地 TCP 监听即可完成 `intake -> restart -> approve -> execute -> complete`
- 错误审批角色在 sandbox-safe smoke 路径中同样会被拒绝

结果：

- 状态：通过
- 备注：适用于本地 TCP 监听被限制的沙箱执行环境
